import { FolderOpen, Music2, X } from 'lucide-react';
interface Props{open:boolean;onClose:()=>void;onOpenMusic:()=>void;}
export default function MediaManager({open,onClose,onOpenMusic}:Props){if(!open)return null;return <div className="fixed inset-0 z-[74] bg-black/50 flex items-center justify-center" onMouseDown={e=>e.currentTarget===e.target&&onClose()}><section className="w-[min(620px,calc(100vw-2rem))] bg-surface border border-border rounded-md shadow-2xl" role="dialog" aria-modal="true"><header className="flex items-center justify-between px-5 py-4 border-b border-border"><div><div className="text-xs text-text-secondary">DAILY USE</div><h2 className="font-semibold">Media Manager</h2></div><button onClick={onClose}><X size={18}/></button></header><div className="p-5 grid md:grid-cols-2 gap-3"><button onClick={onOpenMusic} className="p-4 text-left border border-border rounded-sm hover:bg-surface-raised"><Music2 size={20} className="text-accent"/><div className="font-medium mt-3">Music Player</div><div className="text-xs text-text-secondary mt-1">Manage and play local audio without putting music into LabOS.</div></button><div className="p-4 border border-border rounded-sm"><FolderOpen size={20} className="text-accent"/><div className="font-medium mt-3">Lab media</div><div className="text-xs text-text-secondary mt-1">Project resources and lab media remain in the existing Resources workflow.</div></div></div></section></div>}
