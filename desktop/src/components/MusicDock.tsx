import { useEffect, useMemo, useRef, useState } from 'react';
import { ListMusic, Pause, Play, SkipBack, SkipForward, Trash2, Upload, Volume2, X, Minimize2, Maximize2, Shuffle, Repeat, Repeat1, Plus, Library, FolderPlus, ChevronUp, ChevronDown, Music2 } from 'lucide-react';
import { useToast } from '../contexts/ToastContext';

type Track = { id: string; name: string; blob: Blob; addedAt?: number };
type Playlist = { id: string; name: string; trackIds: string[]; createdAt: number };
type RepeatMode = 'off' | 'all' | 'one';
const DB = 'labos-local-media'; const VERSION = 2; const TRACK_STORE = 'tracks'; const PLAYLIST_STORE = 'playlists';
function openDb(): Promise<IDBDatabase> { return new Promise((resolve, reject) => { const r = indexedDB.open(DB, VERSION); r.onupgradeneeded = () => { const db = r.result; if (!db.objectStoreNames.contains(TRACK_STORE)) db.createObjectStore(TRACK_STORE, { keyPath: 'id' }); if (!db.objectStoreNames.contains(PLAYLIST_STORE)) db.createObjectStore(PLAYLIST_STORE, { keyPath: 'id' }); }; r.onsuccess = () => resolve(r.result); r.onerror = () => reject(r.error); }); }
const readStore = async <T,>(store: string): Promise<T[]> => { const db = await openDb(); return new Promise((resolve, reject) => { const r = db.transaction(store).objectStore(store).getAll(); r.onsuccess = () => resolve(r.result as T[]); r.onerror = () => reject(r.error); }); };
const writeStore = async (store: string, value: unknown) => { const db = await openDb(); return new Promise<void>((resolve, reject) => { const r = db.transaction(store, 'readwrite').objectStore(store).put(value); r.onsuccess = () => resolve(); r.onerror = () => reject(r.error); }); };
const removeStore = async (store: string, id: string) => { const db = await openDb(); return new Promise<void>((resolve, reject) => { const r = db.transaction(store, 'readwrite').objectStore(store).delete(id); r.onsuccess = () => resolve(); r.onerror = () => reject(r.error); }); };
const fmt = (seconds: number) => !Number.isFinite(seconds) ? '0:00' : `${Math.floor(seconds / 60)}:${Math.floor(seconds % 60).toString().padStart(2, '0')}`;
interface Props { onClose: () => void; onMinimize?: () => void; onRestore?: () => void; minimized?: boolean; autoPause: boolean; }
export default function MusicDock({ onClose, onMinimize, onRestore, minimized = false, autoPause }: Props) {
  const { showToast } = useToast(); const audio = useRef<HTMLAudioElement | null>(null); const urls = useRef<Map<string, string>>(new Map());
  const [tracks, setTracks] = useState<Track[]>([]); const [playlists, setPlaylists] = useState<Playlist[]>([]); const [currentId, setCurrentId] = useState<string | null>(null); const [queue, setQueue] = useState<string[]>([]); const [tab, setTab] = useState<'library' | 'queue' | 'playlists'>('library'); const [playing, setPlaying] = useState(false); const [volume, setVolume] = useState(.65); const [duration, setDuration] = useState(0); const [position, setPosition] = useState(0); const [search, setSearch] = useState(''); const [shuffle, setShuffle] = useState(false); const [repeat, setRepeat] = useState<RepeatMode>('off'); const [newPlaylist, setNewPlaylist] = useState(''); const [selectedPlaylistId, setSelectedPlaylistId] = useState<string | null>(null); const [loading, setLoading] = useState(true); const [error, setError] = useState(false);
  const resumeAfterPause = useRef(false); const current = tracks.find(t => t.id === currentId) || null; const visibleTracks = useMemo(() => tracks.filter(t => t.name.toLowerCase().includes(search.toLowerCase())), [tracks, search]); const queueTracks = queue.map(id => tracks.find(t => t.id === id)).filter(Boolean) as Track[]; const selectedPlaylist = playlists.find(p => p.id === selectedPlaylistId) || null; const src = current ? (urls.current.get(current.id) || (urls.current.set(current.id, URL.createObjectURL(current.blob)), urls.current.get(current.id)!)) : '';
  const load = async () => { try { setError(false); const [ts, ps] = await Promise.all([readStore<Track>(TRACK_STORE), readStore<Playlist>(PLAYLIST_STORE)]); ts.sort((a, b) => (a.addedAt || 0) - (b.addedAt || 0)); setTracks(ts); setPlaylists(ps); setCurrentId(id => id || ts[0]?.id || null); } catch { setError(true); } finally { setLoading(false); } };
  useEffect(() => { load(); return () => urls.current.forEach(URL.revokeObjectURL); }, []);
  useEffect(() => { const pause = () => { if (autoPause && audio.current && !audio.current.paused) { resumeAfterPause.current = true; audio.current.pause(); } }; const resume = () => { if (resumeAfterPause.current && audio.current && current) { resumeAfterPause.current = false; audio.current.play().catch(() => undefined); } }; window.addEventListener('labos:pause-music', pause); window.addEventListener('labos:resume-music', resume); return () => { window.removeEventListener('labos:pause-music', pause); window.removeEventListener('labos:resume-music', resume); }; }, [autoPause, current]);
  useEffect(() => { if (audio.current) audio.current.volume = volume; }, [volume]); useEffect(() => { if (!audio.current) return; audio.current.volume = volume; if (!src) { audio.current.removeAttribute('src'); audio.current.load(); return; } audio.current.src = src; audio.current.load(); setPosition(0); setDuration(0); if (playing) audio.current.play().catch(() => setPlaying(false)); }, [src]);
  const playTrack = async (id: string) => { setCurrentId(id); setPlaying(true); setTimeout(() => audio.current?.play().catch(() => setPlaying(false)), 0); }; const toggle = async () => { if (!audio.current || !current) return; if (audio.current.paused) { try { await audio.current.play(); } catch { showToast('Unable to play this track', 'error'); } } else audio.current.pause(); };
  const advance = () => { if (!currentId) return; if (repeat === 'one') { if (audio.current) { audio.current.currentTime = 0; audio.current.play().catch(() => setPlaying(false)); } return; } if (queue.length) { const next = queue[0]; setQueue(q => q.slice(1)); setCurrentId(next); setPlaying(true); return; } if (!tracks.length) return setPlaying(false); const index = tracks.findIndex(t => t.id === currentId); const nextId = shuffle ? tracks[Math.floor(Math.random() * tracks.length)]?.id : tracks[index + 1]?.id || (repeat === 'all' ? tracks[0]?.id : undefined); if (nextId) { setCurrentId(nextId); setPlaying(true); } else setPlaying(false); };
  const previous = () => { const i = currentId ? tracks.findIndex(t => t.id === currentId) : -1; if (i > 0) playTrack(tracks[i - 1].id); }; const addToQueue = (id: string) => setQueue(q => q.includes(id) ? q : [...q, id]); const moveQueue = (id: string, delta: number) => setQueue(q => { const i = q.indexOf(id), j = i + delta; if (i < 0 || j < 0 || j >= q.length) return q; const n = [...q]; [n[i], n[j]] = [n[j], n[i]]; return n; });
  const addFiles = async (e: React.ChangeEvent<HTMLInputElement>) => { try { for (const file of Array.from(e.target.files || [])) await writeStore(TRACK_STORE, { id: crypto.randomUUID(), name: file.name, blob: file, addedAt: Date.now() + Math.random() }); await load(); } catch { showToast('Unable to import audio', 'error'); } e.target.value = ''; };
  const remove = async (id: string) => { try { if (id === currentId) audio.current?.pause(); await removeStore(TRACK_STORE, id); const url = urls.current.get(id); if (url) { URL.revokeObjectURL(url); urls.current.delete(id); } setQueue(q => q.filter(x => x !== id)); const next = playlists.map(p => ({ ...p, trackIds: p.trackIds.filter(x => x !== id) })); await Promise.all(next.map(p => writeStore(PLAYLIST_STORE, p))); setPlaylists(next); if (id === currentId) setCurrentId(null); await load(); } catch { showToast('Unable to remove this track', 'error'); } };
  const createPlaylist = async () => { const name = newPlaylist.trim(); if (!name) return; const p = { id: crypto.randomUUID(), name, trackIds: [], createdAt: Date.now() }; await writeStore(PLAYLIST_STORE, p); setPlaylists(ps => [...ps, p]); setSelectedPlaylistId(p.id); setNewPlaylist(''); }; const addToPlaylist = async (trackId: string, playlistId: string) => { const p = playlists.find(x => x.id === playlistId); if (!p || p.trackIds.includes(trackId)) return; const next = { ...p, trackIds: [...p.trackIds, trackId] }; await writeStore(PLAYLIST_STORE, next); setPlaylists(ps => ps.map(x => x.id === p.id ? next : x)); }; const deletePlaylist = async (id: string) => { await removeStore(PLAYLIST_STORE, id); setPlaylists(ps => ps.filter(p => p.id !== id)); if (selectedPlaylistId === id) setSelectedPlaylistId(null); }; const removeFromPlaylist = async (trackId: string) => { if (!selectedPlaylist) return; const next = { ...selectedPlaylist, trackIds: selectedPlaylist.trackIds.filter(x => x !== trackId) }; await writeStore(PLAYLIST_STORE, next); setPlaylists(ps => ps.map(p => p.id === next.id ? next : p)); };
  const art = (small = false) => <div className={`${small ? 'w-10 h-10' : 'w-full aspect-square'} shrink-0 rounded-xl border border-border bg-accent/10 text-accent flex items-center justify-center overflow-hidden`}><Music2 size={small ? 17 : 28} /></div>; const audioElement = <audio ref={audio} onPlay={() => setPlaying(true)} onPause={() => setPlaying(false)} onTimeUpdate={e => setPosition(e.currentTarget.currentTime)} onLoadedMetadata={e => setDuration(e.currentTarget.duration)} onEnded={advance} onError={() => setPlaying(false)} />;
  if (minimized) return <div className="fixed right-[68px] bottom-5 z-[80] group" aria-label="Minimized music player"><div className="absolute right-0 bottom-full mb-2 pointer-events-none opacity-0 translate-y-1 group-hover:opacity-100 group-hover:translate-y-0 transition-all duration-150"><div className="rounded-lg border border-border bg-surface-raised px-3 py-2 shadow-xl whitespace-nowrap"><div className="text-[10px] uppercase tracking-[0.16em] text-text-secondary">Music</div><div className="text-xs font-semibold max-w-48 truncate">{current?.name || 'Nothing playing'}</div></div></div><button type="button" onClick={onRestore} className="relative w-12 h-12 rounded-full border border-border bg-surface-raised text-text-primary shadow-xl flex items-center justify-center hover:bg-surface focus:outline-none focus:ring-2 focus:ring-accent/60" title="Restore music player" aria-label="Restore music player"><Music2 size={19}/>{playing && <span className="absolute -right-0.5 -top-0.5 w-3 h-3 rounded-full bg-accent border-2 border-surface-raised" aria-hidden="true"/>}</button>{audioElement}</div>;
  return <section className="h-full min-h-0 flex flex-col bg-surface" aria-label="Music player">
    <header className="shrink-0 flex items-center justify-between px-4 py-3 border-b border-border bg-surface"><div className="flex items-center gap-3 min-w-0"><div className="w-9 h-9 rounded-lg bg-accent/10 text-accent flex items-center justify-center"><Music2 size={18}/></div><div className="min-w-0"><div className="text-[10px] uppercase tracking-[0.18em] text-text-secondary">Media</div><div className="font-semibold truncate">Music Player</div><div className="text-[10px] text-text-secondary">{tracks.length} tracks · {playlists.length} playlists</div></div></div><div className="flex items-center gap-1"><button type="button" onClick={onMinimize} className="p-2 rounded-lg text-text-secondary hover:text-text-primary hover:bg-surface-raised focus:outline-none focus:ring-2 focus:ring-accent/50" title="Minimize music player" aria-label="Minimize music player"><Minimize2 size={16}/></button><button type="button" onClick={onClose} className="p-2 rounded-lg text-text-secondary hover:text-text-primary hover:bg-surface-raised focus:outline-none focus:ring-2 focus:ring-accent/50" title="Close music player" aria-label="Close music player"><X size={17}/></button></div></header>
    <div className="shrink-0 p-3 border-b border-border bg-surface-raised/20"><div className="rounded-xl border border-border bg-surface-raised/40 p-3"><div className="flex gap-3">{art(true)}<div className="min-w-0 flex-1"><div className="text-[10px] uppercase tracking-[0.16em] text-text-secondary">Now playing</div><div className="text-sm font-semibold truncate mt-0.5">{current?.name || 'Nothing playing'}</div><div className="text-[10px] text-text-secondary mt-0.5">{playing ? 'Playing locally' : 'Paused'}</div></div></div><div className="flex items-center gap-2 mt-3 text-[10px] text-text-secondary"><span>{fmt(position)}</span><input aria-label="Track progress" className="flex-1 accent-current" type="range" min="0" max={duration || 0} step="0.1" value={Math.min(position, duration || 0)} onChange={e => { const v = Number(e.target.value); setPosition(v); if (audio.current) audio.current.currentTime = v; }}/><span>{fmt(duration)}</span></div><div className="flex items-center justify-between mt-2"><button type="button" onClick={() => setShuffle(v => !v)} className={`p-2 rounded-lg hover:bg-surface ${shuffle ? 'text-accent' : 'text-text-secondary'}`} aria-label="Toggle shuffle"><Shuffle size={15}/></button><button type="button" onClick={previous} disabled={!currentId} className="p-2 rounded-lg text-text-secondary hover:text-text-primary disabled:opacity-40" aria-label="Previous track"><SkipBack size={16}/></button><button type="button" onClick={toggle} disabled={!current} className="w-10 h-10 rounded-full bg-accent text-bg flex items-center justify-center disabled:opacity-40" aria-label={playing ? 'Pause' : 'Play'}>{playing ? <Pause size={17}/> : <Play size={17}/>}</button><button type="button" onClick={advance} disabled={!currentId} className="p-2 rounded-lg text-text-secondary hover:text-text-primary disabled:opacity-40" aria-label="Next track"><SkipForward size={16}/></button><button type="button" onClick={() => setRepeat(r => r === 'off' ? 'all' : r === 'all' ? 'one' : 'off')} className={`p-2 rounded-lg hover:bg-surface ${repeat !== 'off' ? 'text-accent' : 'text-text-secondary'}`} aria-label="Toggle repeat">{repeat === 'one' ? <Repeat1 size={15}/> : <Repeat size={15}/>}</button></div></div></div>
    <div className="shrink-0 px-3 py-2 border-b border-border flex items-center gap-2"><div className="flex-1 flex items-center gap-2 rounded-lg border border-border bg-surface-raised/30 px-2.5 py-1.5"><Music2 size={14} className="text-text-secondary"/><input value={search} onChange={e => setSearch(e.target.value)} placeholder="Search tracks" aria-label="Search tracks" className="min-w-0 flex-1 bg-transparent text-xs outline-none"/></div><label className="p-2 rounded-lg text-text-secondary hover:text-text-primary hover:bg-surface-raised cursor-pointer" title="Import audio"><Upload size={16}/><input type="file" accept="audio/*" multiple className="hidden" onChange={addFiles}/></label></div>
    <div className="shrink-0 grid grid-cols-3 border-b border-border">{([['library',Library,'Library'],['queue',ListMusic,'Queue'],['playlists',FolderPlus,'Playlists']] as const).map(([id,Icon,label]) => <button key={id} type="button" onClick={() => setTab(id)} className={`px-2 py-2 text-[11px] font-medium flex items-center justify-center gap-1.5 ${tab === id ? 'text-accent border-b-2 border-accent bg-accent/5' : 'text-text-secondary hover:text-text-primary'}`}><Icon size={14}/>{label}</button>)}</div>
    <div className="flex-1 min-h-0 overflow-auto p-3">{loading ? <div className="space-y-2">{[1,2,3,4].map(i => <div key={i} className="h-14 rounded-xl border border-border bg-surface-raised/30 animate-pulse" />)}</div> : error ? <div className="rounded-xl border border-border bg-surface-raised/30 p-5 text-center"><div className="font-semibold text-sm">Unable to load music</div><div className="text-xs text-text-secondary mt-1">Your local library could not be opened.</div><button type="button" onClick={load} className="mt-3 px-3 py-1.5 rounded-lg border border-border text-xs font-medium hover:bg-surface-raised">Try again</button></div> : tab === 'library' ? <div className="space-y-2">{visibleTracks.length ? visibleTracks.map(t => <div key={t.id} className={`flex items-center gap-2.5 p-2.5 rounded-xl border border-border ${currentId === t.id ? 'bg-accent/5 border-accent/30' : 'bg-surface-raised/20 hover:bg-surface-raised/50'}`}>{art(true)}<button type="button" onClick={() => playTrack(t.id)} className="min-w-0 flex-1 text-left"><div className="text-xs font-medium truncate">{t.name}</div><div className="text-[10px] text-text-secondary">{currentId === t.id && playing ? 'Playing' : 'Play track'}</div></button><button type="button" onClick={() => addToQueue(t.id)} className="p-2 rounded-lg text-text-secondary hover:text-text-primary hover:bg-surface" title="Add to queue" aria-label={`Add ${t.name} to queue`}><Plus size={14}/></button><button type="button" onClick={() => remove(t.id)} className="p-2 rounded-lg text-text-secondary hover:text-danger hover:bg-surface" title="Remove track" aria-label={`Remove ${t.name}`}><Trash2 size={14}/></button></div>) : <div className="py-10 text-center text-xs text-text-secondary">{tracks.length ? 'No tracks match your search.' : 'No local tracks yet. Import audio to start your library.'}</div>}</div> : tab === 'queue' ? <div className="space-y-2">{queueTracks.length ? queueTracks.map((t,i) => <div key={t.id} className="flex items-center gap-2 p-2.5 rounded-xl border border-border bg-surface-raised/20"><div className="text-[10px] text-text-secondary w-4">{i+1}</div>{art(true)}<button type="button" onClick={() => playTrack(t.id)} className="min-w-0 flex-1 text-left text-xs font-medium truncate">{t.name}</button><button type="button" onClick={() => moveQueue(t.id,-1)} className="p-1.5 rounded text-text-secondary hover:text-text-primary" aria-label="Move up"><ChevronUp size={14}/></button><button type="button" onClick={() => moveQueue(t.id,1)} className="p-1.5 rounded text-text-secondary hover:text-text-primary" aria-label="Move down"><ChevronDown size={14}/></button></div>) : <div className="py-10 text-center text-xs text-text-secondary">Queue is empty.</div>}</div> : <div className="space-y-2">{playlists.map(p => <div key={p.id} className="rounded-xl border border-border bg-surface-raised/20 p-3"><button type="button" onClick={() => setSelectedPlaylistId(p.id)} className="w-full text-left"><div className="text-xs font-semibold">{p.name}</div><div className="text-[10px] text-text-secondary mt-0.5">{p.trackIds.length} tracks</div></button><button type="button" onClick={() => deletePlaylist(p.id)} className="mt-2 p-1.5 rounded text-text-secondary hover:text-danger" aria-label={`Delete ${p.name}`}><Trash2 size={14}/></button></div>)}<div className="flex gap-2 pt-2"><input value={newPlaylist} onChange={e => setNewPlaylist(e.target.value)} placeholder="New playlist" className="min-w-0 flex-1 rounded-lg border border-border bg-surface-raised px-2.5 py-2 text-xs"/><button type="button" onClick={createPlaylist} className="p-2 rounded-lg bg-accent text-bg" aria-label="Create playlist"><Plus size={15}/></button></div>{selectedPlaylist && <div className="mt-3 rounded-xl border border-border p-3"><div className="text-xs font-semibold mb-2">{selectedPlaylist.name}</div>{selectedPlaylist.trackIds.map(id => { const t = tracks.find(x => x.id === id); return t ? <div key={id} className="flex items-center gap-2 py-1.5 text-xs"><span className="truncate flex-1">{t.name}</span><button type="button" onClick={() => removeFromPlaylist(id)} className="text-text-secondary hover:text-danger" aria-label={`Remove ${t.name} from playlist`}><X size={13}/></button></div> : null; })}</div>}</div>}</div>
    <div className="shrink-0 px-3 py-2 border-t border-border flex items-center gap-2 text-[10px] text-text-secondary"><Volume2 size={14}/><input aria-label="Volume" className="flex-1 accent-current" type="range" min="0" max="1" step="0.01" value={volume} onChange={e => setVolume(Number(e.target.value))}/><span className="w-7 text-right">{Math.round(volume*100)}%</span></div>
    {audioElement}
  </section>;
}
